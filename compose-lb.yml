version: '3.9'

networks:
  yb-dbnet:
    name: yb-dbnet

services:

  traefik-shared:
    restart: unless-stopped
    image: traefik:v2.5.3
    networks:
      - yb-dbnet
    ports:
      - 35432:35432
    volumes:
      - type: bind
        source: ./etc/traefik
        target: /etc/traefik
      - type: bind
        source: /var/run/docker.sock
        target: /var/run/docker.sock
